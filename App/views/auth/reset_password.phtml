<?php
$pageTitle = 'Nueva Contraseña | SIRES';
$pageCss   = ['auth.css'];
include __DIR__ . '/../partials/head.phtml';
?>

    <div style="display: flex; justify-content: center; align-items: center; min-height: 100vh; width: 100vw; padding: 20px; box-sizing: border-box;">

        <div class="auth-card">
            <div class="auth-header">
                <h2>Nueva contraseña</h2>
                <div class="auth-icon-circle">
                    <i class="fa-solid fa-lock"></i>
                </div>
                <p>Ingresa tu nueva contraseña</p>
            </div>

            <?php if (!empty($errorMessage)): ?>
                <div class="alert alert--error">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    <span><?= htmlspecialchars($errorMessage) ?></span>
                </div>
            <?php endif; ?>

            <!-- Pasando el procesamiento del formulario por la función global url() -->
            <form action="<?= url('/password/reset/process') ?>" method="POST" autocomplete="off" novalidate>
                <input type="hidden" name="token" value="<?= htmlspecialchars($token ?? '') ?>">

                <div class="form__group">
                    <label class="form__label">NUEVA CONTRASEÑA</label>
                    <div class="form__password">
                        <input type="password" id="password" name="password" class="form__input" placeholder="••••••••" required>
                        <i class="fa-regular fa-eye" onclick="togglePasswordVisibility('password', this)"></i>
                    </div>
                </div>

                <div class="form__group">
                    <label class="form__label">CONFIRMAR CONTRASEÑA</label>
                    <div class="form__password">
                        <input type="password" id="confirm_password" name="confirm_password" class="form__input" placeholder="••••••••" required>
                        <i class="fa-regular fa-eye" onclick="togglePasswordVisibility('confirm_password', this)"></i>
                    </div>
                </div>

                <button type="submit" class="btn btn--accent btn--full">Restablecer contraseña</button>
            </form>
        </div>

    </div>

    <script>
        function togglePasswordVisibility(idField, iconElement) {
            const input = document.getElementById(idField);
            if (input.type === "password") {
                input.type = "text";
                iconElement.classList.replace("fa-eye", "fa-eye-slash");
            } else {
                input.type = "password";
                iconElement.classList.replace("fa-eye-slash", "fa-eye");
            }
        }
    </script>

<?php include __DIR__ . '/../partials/footer.phtml'; ?>
