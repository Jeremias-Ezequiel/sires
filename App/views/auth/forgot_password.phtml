<?php
$pageTitle = 'Recuperar Contraseña | SIRES';
$pageCss   = ['auth.css'];
include __DIR__ . '/../partials/head.phtml';
?>

    <div style="display: flex; justify-content: center; align-items: center; min-height: 100vh; width: 100vw; padding: 20px; box-sizing: border-box;">

        <div class="auth-card">
            <div class="auth-header">
                <h2>3. Recuperar contraseña</h2>
                <div class="auth-icon-circle">
                    <i class="fa-regular fa-envelope"></i>
                </div>
                <p>Te enviaremos un enlace para restablecer tu contraseña</p>
            </div>

            <?php if (!empty($errorMessage)): ?>
                <div class="alert alert--error">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    <?= htmlspecialchars($errorMessage) ?>
                </div>
            <?php endif; ?>

            <!-- Pasando el procesamiento del formulario por la función global url() -->
            <form action="<?= url('/password/recovery/process') ?>" method="POST" autocomplete="off" novalidate>
                <div class="form__group">
                    <label class="form__label">EMAIL</label>
                    <input type="email" name="email" class="form__input" placeholder="correo@email.com" required autocomplete="off">
                </div>

                <button type="submit" class="btn btn--accent btn--full">Enviar enlace</button>
            </form>

            <div class="auth-footer-link">
                <!-- Pasando el retorno al login por la función global url() -->
                <a href="<?= url('/login') ?>">Volver al login</a>
            </div>
        </div>

    </div>

<?php include __DIR__ . '/../partials/footer.phtml'; ?>
