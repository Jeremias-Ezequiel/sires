<header class="main-header employees-header">
    <div>
        <h1>Administración de Habitaciones</h1>
        <p>Gestión del inventario de habitaciones del hotel</p>
    </div>

    <div class="header-actions-container">
        <a href="<?= APP_PREFIX ?>/dashboard/habitaciones/add" class="btn-add">
            <i class="fas fa-plus"></i> Registrar Habitación
        </a>
    </div>
</header>

<?php if (!empty($errorMessage)): ?>
    <div class="alert alert-error">
        <i class="fa-solid fa-triangle-exclamation"></i>
        <?= htmlspecialchars($errorMessage) ?>
    </div>
<?php endif; ?>

<?php if (!empty($flashMessage) && !empty($flashStatus)): ?>
    <div class="alert alert-<?= htmlspecialchars($flashStatus) ?>">
        <i class="fa-solid <?= $flashStatus === 'success' ? 'fa-circle-check' : 'fa-triangle-exclamation' ?>"></i>
        <?= htmlspecialchars($flashMessage) ?>
    </div>
<?php endif; ?>

<section class="details-section filter-section">
    <form action="<?= APP_PREFIX ?>/dashboard/habitaciones" method="GET" class="filter-form">

        <div class="form-group-search">
            <label class="label-input">Buscar por N° Habitación</label>
            <div class="search-input-wrapper">
                <i class="fas fa-search search-icon"></i>
                <input type="text" name="search" placeholder="Ej: 101..." value="<?= htmlspecialchars($_GET['search'] ?? '') ?>" class="input-search" autocomplete="off">
            </div>
        </div>

        <div class="form-group-role">
            <label class="label-input">Filtrar por Tipo</label>
            <select name="type_filter" class="select-role">
                <option value="">Todos los Tipos</option>
                <?php if (!empty($tipos)): ?>
                    <?php foreach ($tipos as $tipo): ?>
                        <option value="<?= $tipo['id'] ?>" <?= ($_GET['type_filter'] ?? '') === (string)$tipo['id'] ? 'selected' : '' ?>><?= htmlspecialchars($tipo['descripcion']) ?></option>
                    <?php endforeach; ?>
                <?php endif; ?>
            </select>
        </div>

        <div class="form-group-role">
            <label class="label-input">Filtrar por Estado</label>
            <select name="status_filter" class="select-status">
                <option value="">Todos los Estados</option>
                <?php if (!empty($estados)): ?>
                    <?php foreach ($estados as $estado): ?>
                        <option value="<?= $estado['id'] ?>" <?= ($_GET['status_filter'] ?? '') === (string)$estado['id'] ? 'selected' : '' ?>><?= htmlspecialchars($estado['descripcion']) ?></option>
                    <?php endforeach; ?>
                <?php endif; ?>
            </select>
        </div>

        <div class="form-group-role">
            <label class="label-input">Filtrar por Piso</label>
            <select name="floor_filter" class="select-role">
                <option value="">Todos los Pisos</option>
                <?php if (!empty($pisos)): ?>
                    <?php foreach ($pisos as $piso): ?>
                        <option value="<?= $piso ?>" <?= ($_GET['floor_filter'] ?? '') === (string)$piso ? 'selected' : '' ?>>Piso <?= $piso ?></option>
                    <?php endforeach; ?>
                <?php endif; ?>
            </select>
        </div>

        <button type="submit" class="btn-primary btn-filter">
            <i class="fas fa-filter"></i> Filtrar
        </button>
        <?php if ($hasSearch || $hasStatus || $hasType || $hasFloor): ?>
            <a href="<?= APP_PREFIX ?>/dashboard/habitaciones" class="btn-clear-filters">
                <i class="fas fa-undo"></i>Limpiar Filtros
            </a>
        <?php endif; ?>
    </form>
</section>

<?php if (!empty($habitaciones) && is_array($habitaciones)): ?>
    <section class="details-section">
        <div class="details-table-wrapper">
            <table class="details-table">
                <thead>
                    <tr>
                        <th class="col-id">N°</th>
                        <th>Piso</th>
                        <th>Tipo</th>
                        <th>Estado</th>
                        <th>Precio/Noche</th>
                        <th class="col-actions">Detalle</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($habitaciones as $room):
                        $statusLower = mb_strtolower($room['estado'] ?? '', 'UTF-8');
                        $statusClass = (strpos($statusLower, 'ocupad') !== false) ? 'status-ocupada' : ((strpos($statusLower, 'mantenimient') !== false || strpos($statusLower, 'bloquead') !== false) ? 'status-reservada' : 'status-disponible');
                    ?>
                        <tr>
                            <td><strong>#<?= htmlspecialchars((string)$room['numero']) ?></strong></td>
                            <td><?= htmlspecialchars((string)$room['piso']) ?></td>
                            <td><?= htmlspecialchars($room['tipo']) ?></td>
                            <td><span class="status-badge <?= $statusClass ?>"><?= htmlspecialchars($room['estado']) ?></span></td>
                            <td>$<?= number_format((float)($room['precio_noche_base'] ?? 0), 2, ',', '.') ?></td>
                            <td class="cell-actions">
                                <div class="actions-group-container">
                                    <a href="<?= APP_PREFIX ?>/maintenance" class="emp-action-btn btn-edit-hover" title="Editar Habitación">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="<?= APP_PREFIX ?>/maintenance" class="emp-action-btn btn-pass-hover" title="Ver Detalle">
                                        <i class="fas fa-info-circle"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </section>
<?php else: ?>
    <section class="details-section no-results-container">
        <div class="no-results-content">
            <div class="no-results-icon-frame">
                <i class="fas fa-bed"></i>
            </div>
            <h3>No se encontraron habitaciones</h3>
            <p>No existen registros que coincidan con los criterios de búsqueda especificados.</p>

            <?php if (!empty($_GET['search']) || !empty($_GET['type_filter']) || !empty($_GET['status_filter']) || !empty($_GET['floor_filter'])): ?>
                <a href="<?= APP_PREFIX ?>/dashboard/habitaciones" class="btn-clear-filters">
                    <i class="fas fa-undo"></i> Limpiar filtros de búsqueda
                </a>
            <?php endif; ?>
        </div>
    </section>
<?php endif; ?>