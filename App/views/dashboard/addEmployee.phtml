<header class="main-header employees-header">
    <div>
        <h1>Registrar Nuevo Empleado</h1>
        <p>Alta de personal y configuración de accesos al sistema SIRES</p>
    </div>

    <a href="/sires/dashboard/employees" class="btn-clear-filters">
        <i class="fas fa-arrow-left"></i> Volver a la Lista
    </a>
</header>

<!-- Contenedor dinámico para alertas por JavaScript (Front) o redirecciones PHP (Back) -->
<div id="alert-container">
    <?php if (!empty($flashMessage) && !empty($flashStatus)): ?>
        <div class="alert alert-<?= htmlspecialchars($flashStatus) ?>">
            <i class="fa-solid <?= $flashStatus === 'success' ? 'fa-circle-check' : 'fa-triangle-exclamation' ?>"></i>
            <?= htmlspecialchars($flashMessage) ?>
        </div>
    <?php endif; ?>
</div>

<section class="details-section form-container-section">
    <form action="/sires/dashboard/employees/add/process" method="POST" class="employee-form">

        <div class="form-section-title">
            <i class="fas fa-user"></i> Datos Personales
        </div>

        <div class="form-grid-layout">
            <div class="form-field-group">
                <label class="label-input" for="nombre">Nombre</label>
                <input type="text" id="nombre" name="nombre" placeholder="Ej: Juan" required class="input-form-field" autocomplete="off" value="<?= htmlspecialchars($_POST['nombre'] ?? $old['nombre'] ?? '') ?>"
                    pattern="^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$"
                    title="El nombre solo puede contener letras y espacios.">
            </div>

            <div class="form-field-group">
                <label class="label-input" for="apellido">Apellido</label>
                <input type="text" id="apellido" name="apellido" placeholder="Ej: Pérez" required class="input-form-field" autocomplete="off" value="<?= htmlspecialchars($_POST['apellido'] ?? $old['apellido'] ?? '') ?>"
                    pattern="^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$"
                    title="El apellido solo puede contener letras y espacios.">
            </div>
        </div>

        <div class="form-section-title">
            <i class="fas fa-map-marker-alt"></i> Ubicación y Procedencia
        </div>

        <div class="form-grid-layout grid-three-columns">
            <div class="form-field-group">
                <label class="label-input" for="id_nacionalidad">Nacionalidad</label>
                <select id="id_nacionalidad" name="id_nacionalidad" required class="select-form-field">
                    <option value="" disabled <?= !isset($_POST['id_nacionalidad']) && !isset($old['id_nacionalidad']) ? 'selected' : '' ?>>Seleccione...</option>
                    <?php if (!empty($countries)): ?>
                        <?php foreach ($countries as $cou): ?>
                            <?php $current_cou_id = $_POST['id_nacionalidad'] ?? $old['id_nacionalidad'] ?? ''; ?>
                            <option value="<?= $cou->getId() ?>" <?= (string)$current_cou_id === (string)$cou->getId() ? 'selected' : '' ?>><?= htmlspecialchars($cou->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div class="form-field-group">
                <label class="label-input" for="id_provincia">Provincia</label>
                <select id="id_provincia" name="id_provincia" required class="select-form-field" <?= !isset($_POST['id_nacionalidad']) && !isset($old['id_nacionalidad']) ? 'disabled' : '' ?>>
                    <option value="" disabled <?= !isset($_POST['id_provincia']) && !isset($old['id_provincia']) ? 'selected' : '' ?>>Seleccione...</option>
                    <?php if (!empty($provinces)): ?>
                        <?php foreach ($provinces as $prov): ?>
                            <?php $current_prov_id = $_POST['id_provincia'] ?? $old['id_provincia'] ?? ''; ?>
                            <option value="<?= $prov->getId() ?>" <?= (string)$current_prov_id === (string)$prov->getId() ? 'selected' : '' ?>><?= htmlspecialchars($prov->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div class="form-field-group">
                <label class="label-input" for="id_localidad">Localidad</label>
                <select id="id_localidad" name="id_localidad" required class="select-form-field" <?= !isset($_POST['id_provincia']) && !isset($old['id_provincia']) ? 'disabled' : '' ?>>
                    <option value="" disabled <?= !isset($_POST['id_localidad']) && !isset($old['id_localidad']) ? 'selected' : '' ?>>Seleccione...</option>
                    <?php if (!empty($cities)): ?>
                        <?php foreach ($cities as $cit): ?>
                            <?php $current_cit_id = $_POST['id_localidad'] ?? $old['id_localidad'] ?? ''; ?>
                            <option value="<?= $cit->getId() ?>" <?= (string)$current_cit_id === (string)$cit->getId() ? 'selected' : '' ?>><?= htmlspecialchars($cit->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>
        </div>

        <div class="form-section-title">
            <i class="fas fa-shield-alt"></i> Credenciales y Permisos
        </div>

        <!-- Fila Superior: Correo y Rol -->
        <div class="form-grid-layout grid-three-columns">
            <div class="form-field-group">
                <label class="label-input" for="email">Correo Electrónico</label>
                <input type="email" id="email" name="email" placeholder="ejemplo@sires.com" required class="input-form-field" autocomplete="off" value="<?= htmlspecialchars($_POST['email'] ?? $old['email'] ?? '') ?>">
            </div>

            <div class="form-field-group">
                <label class="label-input" for="id_rol">Rol del Empleado</label>
                <select id="id_rol" name="id_rol" required class="select-form-field">
                    <option value="" disabled <?= !isset($_POST['id_rol']) && !isset($old['id_rol']) ? 'selected' : '' ?>>Seleccione un Rol</option>
                    <?php if (!empty($roles)): ?>
                        <?php foreach ($roles as $rol): ?>
                            <?php $current_role_id = $_POST['id_rol'] ?? $old['id_rol'] ?? ''; ?>
                            <option value="<?= $rol->getId() ?>" <?= (string)$current_role_id === (string)$rol->getId() ? 'selected' : '' ?>><?= htmlspecialchars($rol->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div></div>
        </div>

        <!-- Fila Inferior: Contraseñas desglosadas -->
        <div class="form-grid-layout grid-three-columns" style="margin-top: 15px;">
            <div class="form-field-group">
                <label class="label-input" for="password">Contraseña</label>
                <div class="password-wrapper" style="position: relative; width: 100%;">
                    <input type="password" id="password" name="password" placeholder="Mínimo 5 carac." required class="input-form-field">
                    <i class="fa-solid fa-eye" id="togglePassword" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); cursor: pointer; color: var(--texto-gris);"></i>
                </div>
            </div>

            <div class="form-field-group">
                <label class="label-input" for="confirm_password">Confirmar Contraseña</label>
                <div class="password-wrapper" style="position: relative; width: 100%;">
                    <input type="password" id="confirm_password" name="confirm_password" placeholder="Repetir clave" required class="input-form-field">
                    <i class="fa-solid fa-eye" id="toggleConfirmPassword" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); cursor: pointer; color: var(--texto-gris);"></i>
                </div>
            </div>

            <div></div>
        </div>

        <div class="form-actions-footer">
            <button type="submit" class="btn-filter">
                <i class="fas fa-save"></i> Guardar
            </button>
        </div>

    </form>
</section>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const formulario = document.querySelector('.employee-form');
        const alertContainer = document.getElementById('alert-container');
        const inputNombre = document.getElementById('nombre');
        const inputApellido = document.getElementById('apellido');
        const inputEmail = document.getElementById('email');
        const inputPass = document.getElementById('password');
        const inputConfirmPass = document.getElementById('confirm_password');
        const selectRol = document.getElementById('id_rol');
        const selectNacionalidad = document.getElementById('id_nacionalidad');
        const selectProvincia = document.getElementById('id_provincia');
        const selectLocalidad = document.getElementById('id_localidad');

        function mostrarError(mensaje) {
            alertContainer.innerHTML = `
                <div class="alert alert-error">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    ${mensaje}
                </div>
            `;
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // --- 👁️ Lógica Ocultar/Mostrar Contraseñas ---
        const setupTogglePassword = (toggleId, inputId) => {
            const toggle = document.querySelector(toggleId);
            const input = document.querySelector(inputId);
            if (toggle && input) {
                toggle.addEventListener('click', function() {
                    const type = input.getAttribute('type') === 'password' ? 'text' : 'password';
                    input.setAttribute('type', type);
                    this.classList.toggle('fa-eye-slash');
                });
            }
        };
        setupTogglePassword('#togglePassword', '#password');
        setupTogglePassword('#toggleConfirmPassword', '#confirm_password');

        // --- 🚫 Validación integral del formulario al enviar ---
        if (formulario && alertContainer) {
            formulario.addEventListener('submit', function(e) {
                alertContainer.innerHTML = '';

                // 1. Nombre
                const nombreVal = inputNombre?.value.trim() ?? '';
                if (nombreVal === '') {
                    e.preventDefault();
                    mostrarError('El nombre es obligatorio.');
                    inputNombre?.focus();
                    return;
                }
                if (!/^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/.test(nombreVal)) {
                    e.preventDefault();
                    mostrarError('El nombre solo puede contener letras y espacios.');
                    inputNombre?.focus();
                    return;
                }

                // 2. Apellido
                const apellidoVal = inputApellido?.value.trim() ?? '';
                if (apellidoVal === '') {
                    e.preventDefault();
                    mostrarError('El apellido es obligatorio.');
                    inputApellido?.focus();
                    return;
                }
                if (!/^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/.test(apellidoVal)) {
                    e.preventDefault();
                    mostrarError('El apellido solo puede contener letras y espacios.');
                    inputApellido?.focus();
                    return;
                }

                // 3. Nacionalidad
                if (!selectNacionalidad || selectNacionalidad.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una nacionalidad.');
                    selectNacionalidad?.focus();
                    return;
                }

                // 4. Provincia
                if (!selectProvincia || selectProvincia.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una provincia.');
                    selectProvincia?.focus();
                    return;
                }

                // 5. Localidad
                if (!selectLocalidad || selectLocalidad.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una localidad.');
                    selectLocalidad?.focus();
                    return;
                }

                // 6. Correo Electrónico
                const emailVal = inputEmail?.value.trim() ?? '';
                if (emailVal === '') {
                    e.preventDefault();
                    mostrarError('El correo electrónico es obligatorio.');
                    inputEmail?.focus();
                    return;
                }
                const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
                if (!emailRegex.test(emailVal)) {
                    e.preventDefault();
                    mostrarError('El formato del correo electrónico no es válido (ej: nombre@dominio.com).');
                    inputEmail?.focus();
                    return;
                }

                // 7. Rol
                if (!selectRol || selectRol.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar un rol para el empleado.');
                    selectRol?.focus();
                    return;
                }

                // 8. Contraseña
                const passVal = inputPass?.value ?? '';
                if (passVal === '') {
                    e.preventDefault();
                    mostrarError('La contraseña es obligatoria.');
                    inputPass?.focus();
                    return;
                }
                if (passVal.length < 5) {
                    e.preventDefault();
                    mostrarError('La contraseña debe tener al menos 5 caracteres.');
                    inputPass?.focus();
                    return;
                }

                // 9. Confirmar Contraseña
                const confirmVal = inputConfirmPass?.value ?? '';
                if (confirmVal === '') {
                    e.preventDefault();
                    mostrarError('Debe confirmar la contraseña.');
                    inputConfirmPass?.focus();
                    return;
                }
                if (passVal !== confirmVal) {
                    e.preventDefault();
                    mostrarError('Las contraseñas ingresadas no coinciden. Por favor, verificalas.');
                    inputConfirmPass?.focus();
                    return;
                }
            });
        }

        // --- 🚫 Bloqueo absoluto de números y símbolos en Nombre/Apellido ---
        if (inputNombre && inputApellido) {
            const inputsLetras = [inputNombre, inputApellido];
            inputsLetras.forEach(input => {
                input.addEventListener('keypress', function(e) {
                    const regex = /^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]$/;
                    if (!regex.test(e.key)) e.preventDefault();
                });
                input.addEventListener('input', function() {
                    this.value = this.value.replace(/[^a-zA-ZáéíóúÁÉÍÓÚñÑ\s]/g, '');
                });
                input.addEventListener('paste', function(e) {
                    const dataPegada = (e.clipboardData || window.clipboardData).getData('text');
                    const regexGeneral = /^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/;
                    if (!regexGeneral.test(dataPegada)) {
                        e.preventDefault();
                        mostrarError('No se permite pegar números o símbolos en los campos de nombre o apellido.');
                    }
                });
                input.addEventListener('drop', function(e) { e.preventDefault(); });
            });
        }

        // --- 🌍 Lógica de Dependencia Escalonada de Ubicación ---
        if (selectNacionalidad && selectProvincia && selectLocalidad) {
            selectNacionalidad.addEventListener('change', function() {
                if (this.value !== "") {
                    selectProvincia.disabled = false;
                } else {
                    selectProvincia.disabled = true;
                    selectLocalidad.disabled = true;
                    selectProvincia.value = "";
                    selectLocalidad.value = "";
                }
            });

            selectProvincia.addEventListener('change', function() {
                if (this.value !== "") {
                    selectLocalidad.disabled = false;
                } else {
                    selectLocalidad.disabled = true;
                    selectLocalidad.value = "";
                }
            });
        }
    });
</script>
