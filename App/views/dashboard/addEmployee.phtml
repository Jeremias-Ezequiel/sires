<header class="main-header employees-header">
    <div>
        <h1>Registrar Nuevo Empleado</h1>
        <p>Alta de personal y configuración de accesos al sistema SIRES</p>
    </div>

    <!-- Modificado con la función global url() -->
    <a href="<?= url('/dashboard/employees') ?>" class="btn btn--ghost">
        <i class="fas fa-arrow-left"></i> Volver a la Lista
    </a>
</header>

<!-- Contenedor dinámico para alertas por JavaScript (Front) o redirecciones PHP (Back) -->
<div id="alert-container">
    <?php if (!empty($flashMessage) && !empty($flashStatus)): ?>
        <div class="alert alert--<?= htmlspecialchars($flashStatus) ?>">
            <i class="fa-solid <?= $flashStatus === 'success' ? 'fa-circle-check' : 'fa-triangle-exclamation' ?>"></i>
            <?= htmlspecialchars($flashMessage) ?>
        </div>
    <?php endif; ?>
</div>

<section class="details-section form-container-section">
    <!-- 🌟 Apertura del formulario automatizada para POST: Resuelve la acción a través del UrlHelper nativo -->
    <?= \App\Helpers\UrlHelper::openForm('/dashboard/employees/add/process', 'POST', 'employee-form') ?>

        <div class="form__section-title">
            <i class="fas fa-user"></i> Datos Personales
        </div>

        <div class="form__grid">
            <div class="form__group">
                <label class="form__label" for="nombre">Nombre</label>
                <input type="text" id="nombre" name="nombre" placeholder="Ej: Juan" required class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['nombre'] ?? $old['nombre'] ?? '') ?>"
                    pattern="^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$"
                    title="El nombre solo puede contener letras y espacios.">
            </div>

            <div class="form__group">
                <label class="form__label" for="apellido">Apellido</label>
                <input type="text" id="apellido" name="apellido" placeholder="Ej: Pérez" required class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['apellido'] ?? $old['apellido'] ?? '') ?>"
                    pattern="^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$"
                    title="El apellido solo puede contener letras y espacios.">
            </div>
        </div>

        <div class="form__section-title">
            <i class="fas fa-id-card"></i> Documentación y Contacto
        </div>

        <div class="form__grid form__grid--3">
            <div class="form__group">
                <label class="form__label" for="dni">DNI / Documento</label>
                <input type="text" id="dni" name="dni" placeholder="Ej: 35.123.456" class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['dni'] ?? $old['dni'] ?? '') ?>">
            </div>

            <div class="form__group">
                <label class="form__label" for="cuil">CUIL</label>
                <input type="text" id="cuil" name="cuil" placeholder="Ej: 20-12345678-9" class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['cuil'] ?? $old['cuil'] ?? '') ?>"
                    pattern="[0-9\-]{11,14}" title="Formato CUIL: XX-XXXXXXXX-X">
            </div>

            <div class="form__group">
                <label class="form__label" for="telefono">Teléfono</label>
                <input type="tel" id="telefono" name="telefono" placeholder="Ej: +54 11 1234-5678" class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['telefono'] ?? $old['telefono'] ?? '') ?>">
            </div>
        </div>

        <div class="form__grid">
            <div class="form__group">
                <label class="form__label" for="direccion">Dirección</label>
                <input type="text" id="direccion" name="direccion" placeholder="Ej: Av. San Martín 1234, Piso 2" class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['direccion'] ?? $old['direccion'] ?? '') ?>">
            </div>

            <div class="form__group">
                <label class="form__label" for="fecha_nacimiento">Fecha de Nacimiento</label>
                <input type="date" id="fecha_nacimiento" name="fecha_nacimiento" class="form__input" value="<?= htmlspecialchars($_POST['fecha_nacimiento'] ?? $old['fecha_nacimiento'] ?? '') ?>">
            </div>
        </div>

        <div class="form__section-title">
            <i class="fas fa-map-marker-alt"></i> Ubicación y Procedencia
        </div>

        <div class="form__grid form__grid--3">
            <div class="form__group">
                <label class="form__label" for="id_nacionalidad">Nacionalidad</label>
                <select id="id_nacionalidad" name="id_nacionalidad" required class="form__select">
                    <option value="" disabled <?= !isset($_POST['id_nacionalidad']) && !isset($old['id_nacionalidad']) ? 'selected' : '' ?>>Seleccione...</option>
                    <?php if (!empty($countries)): ?>
                        <?php foreach ($countries as $cou): ?>
                            <?php $current_cou_id = $_POST['id_nacionalidad'] ?? $old['id_nacionalidad'] ?? ''; ?>
                            <option value="<?= $cou->getId() ?>" <?= (string)$current_cou_id === (string)$cou->getId() ? 'selected' : '' ?>><?= htmlspecialchars($cou->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div class="form__group">
                <label class="form__label" for="id_provincia">Provincia</label>
                <select id="id_provincia" name="id_provincia" required class="form__select" disabled>
                    <option value="" disabled selected>Seleccione una Nacionalidad primero...</option>
                </select>
            </div>

            <div class="form__group">
                <label class="form__label" for="id_localidad">Localidad</label>
                <select id="id_localidad" name="id_localidad" required class="form__select" disabled>
                    <option value="" disabled selected>Seleccione una Provincia primero...</option>
                </select>
            </div>
        </div>

        <div class="form__section-title">
            <i class="fas fa-shield-alt"></i> Credenciales y Permisos
        </div>

        <!-- Fila Superior: Correo y Rol -->
        <div class="form__grid form__grid--3">
            <div class="form__group">
                <label class="form__label" for="email">Correo Electrónico</label>
                <input type="email" id="email" name="email" placeholder="ejemplo@sires.com" required class="form__input" autocomplete="off" value="<?= htmlspecialchars($_POST['email'] ?? $old['email'] ?? '') ?>">
            </div>

            <div class="form__group">
                <label class="form__label" for="id_rol">Rol del Empleado</label>
                <select id="id_rol" name="id_rol" required class="form__select">
                    <option value="" disabled <?= !isset($_POST['id_rol']) && !isset($old['id_rol']) ? 'selected' : '' ?>>Seleccione un Rol</option>
                    <?php if (!empty($roles)): ?>
                        <?php foreach ($roles as $rol): ?>
                            <?php $current_role_id = $_POST['id_rol'] ?? $old['id_rol'] ?? ''; ?>
                            <option value="<?= $rol->getId() ?>" <?= (string)$current_role_id === (string)$rol->getId() ? 'selected' : '' ?>><?= htmlspecialchars($rol->getDescripcion()) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div></div>
        </div>

        <!-- Fila Inferior: Contraseñas desglosadas -->
        <div class="form__grid form__grid--3" style="margin-top: 15px;">
            <div class="form__group">
                <label class="form__label" for="password">Contraseña</label>
                <div class="form__password">
                    <input type="password" id="password" name="password" placeholder="Mínimo 5 carac." required class="form__input">
                    <i class="fa-solid fa-eye" id="togglePassword" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); cursor: pointer; color: var(--texto-gris);"></i>
                </div>
            </div>

            <div class="form__group">
                <label class="form__label" for="confirm_password">Confirmar Contraseña</label>
                <div class="form__password">
                    <input type="password" id="confirm_password" name="confirm_password" placeholder="Repetir clave" required class="form__input">
                    <i class="fa-solid fa-eye" id="toggleConfirmPassword" style="position: absolute; right: 12px; top: 50%; transform: translateY(-50%); cursor: pointer; color: var(--texto-gris);"></i>
                </div>
            </div>

            <div></div>
        </div>

        <div class="form__actions">
            <button type="submit" class="btn btn--primary">
                <i class="fas fa-save"></i> Guardar
            </button>
        </div>

    </form>
</section>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const formulario = document.querySelector('.employee-form');
        const alertContainer = document.getElementById('alert-container');
        const inputNombre = document.getElementById('nombre');
        const inputApellido = document.getElementById('apellido');
        const inputEmail = document.getElementById('email');
        const inputPass = document.getElementById('password');
        const inputConfirmPass = document.getElementById('confirm_password');
        const selectRol = document.getElementById('id_rol');
        const selectNacionalidad = document.getElementById('id_nacionalidad');
        const selectProvincia = document.getElementById('id_provincia');
        const selectLocalidad = document.getElementById('id_localidad');

        function mostrarError(mensaje) {
            alertContainer.innerHTML = `
                <div class="alert alert--error">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    \${mensaje}
                </div>
            `;
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // --- 👁️ Lógica Ocultar/Mostrar Contraseñas ---
        const setupTogglePassword = (toggleId, inputId) => {
            const toggle = document.querySelector(toggleId);
            const input = document.querySelector(inputId);
            if (toggle && input) {
                toggle.addEventListener('click', function() {
                    const type = input.getAttribute('type') === 'password' ? 'text' : 'password';
                    input.setAttribute('type', type);
                    this.classList.toggle('fa-eye-slash');
                });
            }
        };
        setupTogglePassword('#togglePassword', '#password');
        setupTogglePassword('#toggleConfirmPassword', '#confirm_password');

        // --- 🚫 Validación integral del formulario al enviar ---
        if (formulario && alertContainer) {
            formulario.addEventListener('submit', function(e) {
                alertContainer.innerHTML = '';

                // 1. Nombre
                const nombreVal = inputNombre?.value.trim() ?? '';
                if (nombreVal === '') {
                    e.preventDefault();
                    mostrarError('El nombre es obligatorio.');
                    inputNombre?.focus();
                    return;
                }
                if (!/^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/.test(nombreVal)) {
                    e.preventDefault();
                    mostrarError('El nombre solo puede contener letras y espacios.');
                    inputNombre?.focus();
                    return;
                }

                // 2. Apellido
                const apellidoVal = inputApellido?.value.trim() ?? '';
                if (apellidoVal === '') {
                    e.preventDefault();
                    mostrarError('El apellido es obligatorio.');
                    inputApellido?.focus();
                    return;
                }
                if (!/^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/.test(apellidoVal)) {
                    e.preventDefault();
                    mostrarError('El apellido solo puede contener letras y espacios.');
                    inputApellido?.focus();
                    return;
                }

                // 3. Nacionalidad
                if (!selectNacionalidad || selectNacionalidad.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una nacionalidad.');
                    selectNacionalidad?.focus();
                    return;
                }

                // 4. Provincia
                if (!selectProvincia || selectProvincia.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una provincia.');
                    selectProvincia?.focus();
                    return;
                }

                // 5. Localidad
                if (!selectLocalidad || selectLocalidad.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar una localidad.');
                    selectLocalidad?.focus();
                    return;
                }

                // 6. Correo Electrónico
                const emailVal = inputEmail?.value.trim() ?? '';
                if (emailVal === '') {
                    e.preventDefault();
                    mostrarError('El correo electrónico es obligatorio.');
                    inputEmail?.focus();
                    return;
                }
                const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
                if (!emailRegex.test(emailVal)) {
                    e.preventDefault();
                    mostrarError('El formato del correo electrónico no es válido (ej: nombre@dominio.com).');
                    inputEmail?.focus();
                    return;
                }

                // 7. Rol
                if (!selectRol || selectRol.value === '') {
                    e.preventDefault();
                    mostrarError('Debe seleccionar un rol para el empleado.');
                    selectRol?.focus();
                    return;
                }

                // 8. Contraseña
                const passVal = inputPass?.value ?? '';
                if (passVal === '') {
                    e.preventDefault();
                    mostrarError('La contraseña es obligatoria.');
                    inputPass?.focus();
                    return;
                }
                if (passVal.length < 5) {
                    e.preventDefault();
                    mostrarError('La contraseña debe tener al menos 5 caracteres.');
                    inputPass?.focus();
                    return;
                }

                // 9. Confirmar Contraseña
                const confirmVal = inputConfirmPass?.value ?? '';
                if (confirmVal === '') {
                    e.preventDefault();
                    mostrarError('Debe confirmar la contraseña.');
                    inputConfirmPass?.focus();
                    return;
                }
                if (passVal !== confirmVal) {
                    e.preventDefault();
                    mostrarError('Las contraseñas ingresadas no coinciden. Por favor, verificalas.');
                    inputConfirmPass?.focus();
                    return;
                }
            });
        }

        // --- 🚫 Bloqueo absoluto de números y símbolos en Nombre/Apellido ---
        if (inputNombre && inputApellido) {
            const inputsLetras = [inputNombre, inputApellido];
            inputsLetras.forEach(input => {
                input.addEventListener('keypress', function(e) {
                    const regex = /^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]$/;
                    if (!regex.test(e.key)) e.preventDefault();
                });
                input.addEventListener('input', function() {
                    this.value = this.value.replace(/[^a-zA-ZáéíóúÁÉÍÓÚñÑ\s]/g, '');
                });
                input.addEventListener('paste', function(e) {
                    const dataPegada = (e.clipboardData || window.clipboardData).getData('text');
                    const regexGeneral = /^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/;
                    if (!regexGeneral.test(dataPegada)) {
                        e.preventDefault();
                        mostrarError('No se permite pegar números o símbolos en los campos de nombre o apellido.');
                    }
                });
                input.addEventListener('drop', function(e) { e.preventDefault(); });
            });
        }

        // --- 🌍 Lógica de Dependencia Escalonada de Ubicación (AJAX) ---
        const baseProvincias  = '<?= \App\Helpers\UrlHelper::to('/api/provincias') ?>';
        const baseLocalidades = '<?= \App\Helpers\UrlHelper::to('/api/localidades') ?>';

        function poblarSelect(select, items, textoDefault) {
            select.innerHTML = '<option value="" disabled selected>' + textoDefault + '</option>';
            items.forEach(function(item) {
                const opt = document.createElement('option');
                opt.value = item.id;
                opt.textContent = item.descripcion;
                select.appendChild(opt);
            });
            select.disabled = false;
        }

        function resetSelect(select, texto) {
            select.innerHTML = '<option value="" disabled selected>' + texto + '</option>';
            select.disabled = true;
        }

        if (selectNacionalidad && selectProvincia && selectLocalidad) {
            selectNacionalidad.addEventListener('change', function() {
                const nacionalidadId = this.value;
                if (nacionalidadId === "") {
                    resetSelect(selectProvincia, 'Seleccione una Nacionalidad primero...');
                    resetSelect(selectLocalidad, 'Seleccione una Provincia primero...');
                    return;
                }
                resetSelect(selectLocalidad, 'Seleccione una Provincia primero...');
                selectProvincia.innerHTML = '<option value="" disabled selected>Cargando...</option>';
                selectProvincia.disabled = true;

                fetch(baseProvincias + '&nacionalidadId=' + encodeURIComponent(nacionalidadId))
                    .then(function(resp) { return resp.json(); })
                    .then(function(data) {
                        if (data.length > 0) {
                            poblarSelect(selectProvincia, data, 'Seleccione...');
                        } else {
                            resetSelect(selectProvincia, 'Sin resultados');
                        }
                    })
                    .catch(function() {
                        resetSelect(selectProvincia, 'Error al cargar');
                    });
            });

            selectProvincia.addEventListener('change', function() {
                const provinciaId = this.value;
                if (provinciaId === "") {
                    resetSelect(selectLocalidad, 'Seleccione una Provincia primero...');
                    return;
                }
                selectLocalidad.innerHTML = '<option value="" disabled selected>Cargando...</option>';
                selectLocalidad.disabled = true;

                fetch(baseLocalidades + '&provinciaId=' + encodeURIComponent(provinciaId))
                    .then(function(resp) { return resp.json(); })
                    .then(function(data) {
                        if (data.length > 0) {
                            poblarSelect(selectLocalidad, data, 'Seleccione...');
                        } else {
                            resetSelect(selectLocalidad, 'Sin resultados');
                        }
                    })
                    .catch(function() {
                        resetSelect(selectLocalidad, 'Error al cargar');
                    });
            });
        }
    });
</script>
