<header class="main-header employees-header">
    <div>
        <h1>Registrar Nueva Habitación</h1>
        <p>Alta de habitaciones en el inventario del hotel</p>
    </div>

    <a href="/sires/dashboard/habitaciones" class="btn-clear-filters">
        <i class="fas fa-arrow-left"></i> Volver a la Lista
    </a>
</header>

<div id="alert-container">
    <?php if (!empty($flashMessage) && !empty($flashStatus)): ?>
        <div class="alert alert-<?= htmlspecialchars($flashStatus) ?>">
            <i class="fa-solid <?= $flashStatus === 'success' ? 'fa-circle-check' : 'fa-triangle-exclamation' ?>"></i>
            <?= htmlspecialchars($flashMessage) ?>
        </div>
    <?php endif; ?>
</div>

<section class="details-section form-container-section">
    <form action="/sires/dashboard/habitaciones/add/process" method="POST" class="employee-form" id="room-form">

        <div class="form-section-title">
            <i class="fas fa-door-open"></i> Ubicación de la Habitación
        </div>

        <div class="form-grid-layout">
            <div class="form-field-group">
                <label class="label-input" for="numero">Número de Habitación</label>
                <input type="number" id="numero" name="numero" placeholder="Ej: 101" required class="input-form-field" autocomplete="off" min="1" value="<?= htmlspecialchars($_POST['numero'] ?? $old['numero'] ?? '') ?>">
            </div>

            <div class="form-field-group">
                <label class="label-input" for="piso">Piso</label>
                <input type="number" id="piso" name="piso" placeholder="Ej: 1" required class="input-form-field" autocomplete="off" min="0" value="<?= htmlspecialchars($_POST['piso'] ?? $old['piso'] ?? '') ?>">
            </div>
        </div>

        <div class="form-section-title">
            <i class="fas fa-tags"></i> Clasificación
        </div>

        <div class="form-grid-layout grid-three-columns">
            <div class="form-field-group">
                <label class="label-input" for="id_tipo_habitacion">Tipo de Habitación</label>
                <select id="id_tipo_habitacion" name="id_tipo_habitacion" required class="select-form-field">
                    <option value="" disabled <?= !isset($_POST['id_tipo_habitacion']) && !isset($old['id_tipo_habitacion']) ? 'selected' : '' ?>>Seleccione un Tipo</option>
                    <?php if (!empty($tipos)): ?>
                        <?php foreach ($tipos as $tipo): ?>
                            <?php $current_tipo_id = $_POST['id_tipo_habitacion'] ?? $old['id_tipo_habitacion'] ?? ''; ?>
                            <option value="<?= $tipo['id'] ?>" <?= (string)$current_tipo_id === (string)$tipo['id'] ? 'selected' : '' ?>><?= htmlspecialchars($tipo['descripcion']) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div class="form-field-group">
                <label class="label-input" for="id_estado_habitacion">Estado Inicial</label>
                <select id="id_estado_habitacion" name="id_estado_habitacion" required class="select-form-field">
                    <option value="" disabled <?= !isset($_POST['id_estado_habitacion']) && !isset($old['id_estado_habitacion']) ? 'selected' : '' ?>>Seleccione un Estado</option>
                    <?php if (!empty($estados)): ?>
                        <?php foreach ($estados as $estado): ?>
                            <?php $current_estado_id = $_POST['id_estado_habitacion'] ?? $old['id_estado_habitacion'] ?? ''; ?>
                            <option value="<?= $estado['id'] ?>" <?= (string)$current_estado_id === (string)$estado['id'] ? 'selected' : '' ?>><?= htmlspecialchars($estado['descripcion']) ?></option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>

            <div class="form-field-group">
                <label class="label-input" for="precio_noche_base">Precio por Noche ($)</label>
                <input type="number" id="precio_noche_base" name="precio_noche_base" placeholder="Ej: 15000.00" required class="input-form-field" autocomplete="off" min="0" step="0.01" value="<?= htmlspecialchars($_POST['precio_noche_base'] ?? $old['precio_noche_base'] ?? '') ?>">
            </div>
        </div>

        <div class="form-actions-footer">
            <button type="submit" class="btn-filter">
                <i class="fas fa-save"></i> Guardar Habitación
            </button>
        </div>

    </form>
</section>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const inputNumero = document.getElementById('numero');
        const inputPiso = document.getElementById('piso');
        const inputPrecio = document.getElementById('precio_noche_base');

        if (inputNumero) {
            inputNumero.addEventListener('keypress', function(e) {
                if (!/[0-9]/.test(e.key)) e.preventDefault();
            });
            inputNumero.addEventListener('input', function() {
                this.value = this.value.replace(/[^0-9]/g, '');
            });
            inputNumero.addEventListener('paste', function(e) {
                const dataPegada = (e.clipboardData || window.clipboardData).getData('text');
                if (!/^[0-9]+$/.test(dataPegada)) {
                    e.preventDefault();
                    document.getElementById('alert-container').innerHTML = `
                        <div class="alert alert-error">
                            <i class="fa-solid fa-triangle-exclamation"></i>
                            Solo se permiten números enteros en el número de habitación.
                        </div>
                    `;
                    window.scrollTo({
                        top: 0,
                        behavior: 'smooth'
                    });
                }
            });
        }

        if (inputPiso) {
            inputPiso.addEventListener('keypress', function(e) {
                if (!/[0-9]/.test(e.key)) e.preventDefault();
            });
            inputPiso.addEventListener('input', function() {
                this.value = this.value.replace(/[^0-9]/g, '');
            });
            inputPiso.addEventListener('paste', function(e) {
                const dataPegada = (e.clipboardData || window.clipboardData).getData('text');
                if (!/^[0-9]+$/.test(dataPegada)) {
                    e.preventDefault();
                    document.getElementById('alert-container').innerHTML = `
                        <div class="alert alert-error">
                            <i class="fa-solid fa-triangle-exclamation"></i>
                            Solo se permiten números enteros en el piso.
                        </div>
                    `;
                    window.scrollTo({
                        top: 0,
                        behavior: 'smooth'
                    });
                }
            });
        }

        if (inputPrecio) {
            inputPrecio.addEventListener('keypress', function(e) {
                if (!/[0-9.]/.test(e.key)) e.preventDefault();
            });
            inputPrecio.addEventListener('input', function() {
                this.value = this.value.replace(/[^0-9.]/g, '');
            });
            inputPrecio.addEventListener('paste', function(e) {
                const dataPegada = (e.clipboardData || window.clipboardData).getData('text');
                if (!/^[0-9]+(\.[0-9]{1,2})?$/.test(dataPegada)) {
                    e.preventDefault();
                    document.getElementById('alert-container').innerHTML = `
                        <div class="alert alert-error">
                            <i class="fa-solid fa-triangle-exclamation"></i>
                            Solo se permiten valores numéricos en el precio (ej: 15000 o 15000.50).
                        </div>
                    `;
                    window.scrollTo({
                        top: 0,
                        behavior: 'smooth'
                    });
                }
            });
        }
    });
</script>
