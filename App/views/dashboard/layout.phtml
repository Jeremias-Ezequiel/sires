<?php

use App\Middleware\AuthMiddleware;
use App\Models\Rol;

$pageTitle = $pageTitle ?? 'Dashboard | SIRES';
$pageCss   = $pageCss ?? ['dashboard.css?v=1.1'];
include __DIR__ . '/../partials/head.phtml';
?>

    <div class="dashboard-container">

        <aside class="sidebar">
            <div class="logo-container">
                <!-- Cargando el logo mediante la función global asset() -->
                <img src="<?= asset('img/login/logo.png') ?>" alt="Logo SIRES" class="logo-sires">
            </div>
            <nav>
                <ul class="nav-links">
                    <li>
                        <!-- Rutas adaptadas con la función global url() -->
                        <a href="<?= url('/dashboard') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'home.phtml')) ? 'active' : '' ?>">
                            <i class="fas fa-home"></i>Inicio
                        </a>
                    </li>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= url('/maintenance') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'clientes.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-users"></i>Clientes
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE])): ?>
                        <li>
                            <a href="<?= url('/dashboard/employees') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'employees.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-user-tie"></i>Empleados
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= url('/maintenance') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'reservas.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-calendar-alt"></i>Reservas
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= url('/dashboard/rooms') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'habitaciones.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-bed"></i>Habitaciones
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE])): ?>
                        <li>
                            <a href="<?= url('/maintenance') ?>" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'pagos.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-money-bill-wave"></i>Pagos
                            </a>
                        </li>
                    <?php endif; ?>

                    <li>
                        <a href="<?= url('/logout') ?>" class="nav-link" style="color: #ef4444;">
                            <i class="fas fa-sign-out-alt"></i>Cerrar Sesión
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <main class="main-content">
            <?php
            // Acá se va a renderizar la vista específica que le mande el controlador (Se mantienen las rutas nativas en disco)
            if (isset($contentView) && file_exists($contentView)) {
                require_once $contentView;
            }
            ?>
        </main>

    </div>

<?php include __DIR__ . '/../partials/footer.phtml'; ?>
