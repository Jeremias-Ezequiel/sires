<?php

use App\Middleware\AuthMiddleware;
use App\Models\Rol;
?>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIRES - Dashboard Hotelero</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="<?= APP_PREFIX ?>/public/css/global.css">
    <link rel="stylesheet" href="<?= APP_PREFIX ?>/public/css/login.css">
    <link rel="stylesheet" href="<?= APP_PREFIX ?>/public/css/dashboard.css?v=1.1">
</head>

<body>

    <div class="dashboard-container">

        <aside class="sidebar">
            <div class="logo-container">
                <img src="<?= APP_PREFIX ?>/public/img/login/logo.png" alt="Logo SIRES" class="logo-sires">
            </div>
            <nav>
                <ul class="nav-links">
                    <li>
                        <a href="<?= APP_PREFIX ?>/dashboard" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'home.phtml')) ? 'active' : '' ?>">
                            <i class="fas fa-home"></i>Inicio
                        </a>
                    </li>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= APP_PREFIX ?>/maintenance" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'clientes.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-users"></i>Clientes
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE])): ?>
                        <li>
                            <a href="<?= APP_PREFIX ?>/dashboard/employees" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'employees.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-user-tie"></i>Empleados
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= APP_PREFIX ?>/maintenance" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'reservas.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-calendar-alt"></i>Reservas
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE, Rol::RECEPCIONISTA])): ?>
                        <li>
                            <a href="<?= APP_PREFIX ?>/maintenance" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'habitaciones.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-bed"></i>Habitaciones
                            </a>
                        </li>
                    <?php endif; ?>

                    <?php if (AuthMiddleware::hasRole([Rol::ADMINISTRADOR, Rol::GERENTE])): ?>
                        <li>
                            <a href="<?= APP_PREFIX ?>/maintenance" class="nav-link <?= (isset($contentView) && str_contains($contentView, 'pagos.phtml')) ? 'active' : '' ?>">
                                <i class="fas fa-money-bill-wave"></i>Pagos
                            </a>
                        </li>
                    <?php endif; ?>

                    <li>
                        <a href="<?= APP_PREFIX ?>/logout" class="nav-link" style="color: #ef4444;">
                            <i class="fas fa-sign-out-alt"></i>Cerrar Sesión
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <main class="main-content">
            <?php
            // Acá se va a renderizar la vista específica que le mande el controlador
            if (isset($contentView) && file_exists($contentView)) {
                require_once $contentView;
            }
            ?>
        </main>

    </div>

</body>

</html>
